# MD to PPT 功能开发计划

## 总览

当前项目核心功能已完整（标题/列表/表格/代码块/引述/Mermaid 图表/块级公式/自动分页），以下为可扩展的功能方向。

| 优先级 | 功能 | 用户价值 | 开发量 | 说明 |
|--------|------|----------|--------|------|
| ★★★ | **链接** `[text](url)` | 高 — Markdown 基础语法 | 低 | PptxGenJS 原生支持 hyperlink，改动仅 parseInlineMarkdown |
| ★★★ | **行内公式** `$...$` | 高 — 技术文档刚需 | 中 | parser 已识别，MathJax 已就绪，难点在 PptxGenJS 文字+图片混排 |
| ★★☆ | **图片** `![]()` | 高 — 文档配图常见 | 中 | image-size 库已有，需处理路径解析和尺寸适配 |
| ★★☆ | **外部模板** `--template` | 高 — 企业/个性化场景 | 中高 | JSZip 解析 .pptx 提取布局，现有生成层不变 |
| ★☆☆ | **主题/配色** | 中 — 多场景适配需求 | 低 | 提取硬编码颜色为主题对象，加 CLI 选项 |
| ★☆☆ | **目录页** | 低 — 锦上添花 | 中 | 需两遍生成或后置插入页码 |

**推荐顺序**：链接 → 行内公式 → 图片 → 外部模板 → 主题 → 目录页

> 链接改动最小（仅 `utils.js`），完成后行内公式可复用其文本段拆分逻辑，图片独立开发，外部模板与前三者无依赖关系可随时启动。

---

## 优先级 1：链接支持 `[text](url)`

**现状**：URL 被丢弃，只保留文本。
**目标**：文本保留为蓝色带下划线，添加超链接。

### 改动点

| 文件 | 操作 |
|------|------|
| `scripts/utils.js` | `parseInlineMarkdown()` 增加 `[text](url)` 正则匹配 |

### 实现思路

```javascript
// parseInlineMarkdown 中增加链接处理
{ text: "点击查看", options: { color: "0563C1", underline: true, hyperlink: { url: "https://..." } } }
```

复杂度低，PptxGenJS 原生支持 `hyperlink` 属性。

---

## 优先级 2：行内公式 `$...$` 渲染

**现状**：`math_formula_parser.js` 已能识别 `$...$`，但 converter.js 跳过渲染，原样输出文本。
**目标**：行内公式通过 MathJax 渲染为 SVG，嵌入到文本行中。

### 改动点

| 文件 | 操作 |
|------|------|
| `scripts/math_formula_parser.js` | 确保 `detectFormulas()` 返回行内公式的位置和内容 |
| `scripts/math_renderer.js` | 复用现有 `render()` 方法，调整 SVG 尺寸适配行内高度 |
| `scripts/converter.js` | 解析段落/列表/表格时，检测 `$...$` → 调用渲染 → 替换为图片 |
| `scripts/utils.js` | `parseInlineMarkdown()` 增加公式片段处理 |
| `test/math_renderer.test.js` | 增加行内公式测试用例 |

### 实现思路

```
段落文本 "$E=mc^2$ 是著名公式"
  ↓ detectInlineFormulas()
[ { text: "是著名公式", formula: "E=mc^2", position: 0 } ]
  ↓ MathRenderer.render("E=mc^2")
{ format: 'svg', data: 'data:image/svg+xml;base64,...', width, height }
  ↓ PptxGenJS addText + addImage 组合
行内图片与文字混排
```

**难点**：PptxGenJS 不支持文字+图片混排，需要将行拆分为多个文本段和图片段，用坐标计算水平排列。

---

## 优先级 3：图片支持 `![]()`

**现状**：完全不支持。
**目标**：支持本地图片路径，自动适配内容区尺寸。

### 改动点

| 文件 | 操作 |
|------|------|
| `scripts/converter.js` | parse() 中检测 `![alt](path)` 语法 |
| `scripts/layout_engine.js` | addImage() 方法，计算图片在内容区的位置和尺寸 |
| `scripts/height_calculator.js` | 图片高度计算（读取图片尺寸后按比例缩放） |
| `scripts/utils.js` | `calculateBestImageSize()` 复用现有方法 |

### 支持范围

- 本地相对路径：`./images/photo.png` → 基于 md 文件路径解析
- 本地绝对路径：`/Users/xxx/photo.png`
- 不支持网络 URL（需下载，增加复杂度）
- 格式：PNG、JPG、SVG（base64 嵌入）

### 图片尺寸策略

1. 读取图片原始尺寸（`image-size` 库，已有依赖）
2. 限制最大宽度为内容区宽度（竖版 ~6.5"、横版 ~9"）
3. 等比缩放，高度不超过内容区剩余空间的 60%
4. 超过限制时分页，图片在新页面居中

---

## 优先级 4：主题/配色自定义

**现状**：颜色硬编码在 `config.js` 和 `converter.js` 中。
**目标**：通过 CLI 选项或配置文件自定义主题色。

### 改动点

| 文件 | 操作 |
|------|------|
| `scripts/config.js` | 提取颜色为主题对象，支持预设主题 |
| `scripts/md_to_ppt.js` | 增加 `--theme` 选项 |
| `scripts/converter.js` | 从配置读取颜色，替代硬编码 |

### 预设主题示例

| 主题 | 标题背景 | 引述背景 | 代码背景 | 主题色 |
|------|----------|----------|----------|--------|
| 默认 | `4472C4` | `E8F4FD` | `F5F5F5` | `4A90E2` |
| 深色 | `2D2D2D` | `1A1A2E` | `333333` | `E94560` |
| 暖色 | `D4A373` | `FEFAE0` | `F5F0EB` | `CCD5AE` |

---

## 优先级 5：目录页自动生成

**现状**：不支持。
**目标**：在封面页后自动生成 TOC 页，列出所有 `##` 级标题。

### 实现思路

1. `parse()` 阶段收集所有 `##` 标题及其页码
2. `generate()` 阶段在封面页后插入 TOC 页
3. TOC 格式：编号 + 标题文本 + 页码（右对齐）

**难点**：页码需要 generate 完成后才能确定，可能需要两遍生成或后置插入。

---

## 优先级 4.5：外部 PPT 模板支持 `--template`

**现状**：布局由 `config.js` 硬编码，`master_slide.js` 用 `defineSlideMaster()` 程序化生成。用户无法指定外部 .pptx 文件作为模板。
**目标**：用户通过 `--template template.pptx` 指定模板文件，工具自动提取布局信息，生成时使用模板的样式。

### 技术调研

**PptxGenJS 限制**：纯生成库，不支持加载/读取已有 .pptx 文件。库作者明确不会支持。

**可行方案对比**：

| 方案 | 原理 | 复杂度 | 模板还原度 |
|------|------|--------|------------|
| **A. JSZip 解析模板 → 提取布局** | 解压 .pptx，读取 slideLayout XML，提取占位符位置/颜色/字号，转为 LAYOUT_CONFIG | 中 | 中 — 简单布局可还原，渐变/阴影无法还原 |
| **B. 换用 pptx-automizer** | 替代 PptxGenJS，原生支持加载模板并修改 | 高（重写） | 高 — 完美还原模板 |
| **C. 混合方案** | PptxGenJS 生成内容 → JSZip 后处理注入模板的 slideMaster/theme XML | 中高 | 高 — 模板还原度好 |

**推荐方案 A**：改动范围最小，现有 PptxGenJS 生成层完全不变，只增加模板解析层。

### 实现流程

```
用户模板 template.pptx
  ↓ JSZip 解压
  ↓ 解析 ppt/slideLayouts/slideLayoutN.xml
  ↓ 提取占位符 <p:ph type="title"/"body"/"ctrTitle"/> 的 x, y, w, h
  ↓ 提取背景色、边框、字号等样式
  ↓ 生成 LAYOUT_CONFIG 对象（与现有 config.js 格式一致）
  ↓ 现有 PptxGenJS 流程不变
输出 .pptx
```

### 改动点

| 文件 | 操作 |
|------|------|
| 新增 `scripts/template_parser.js` | JSZip 解压 .pptx，解析 slideLayout XML，输出 LAYOUT_CONFIG |
| `scripts/md_to_ppt.js` | 增加 `--template <file>` 选项 |
| `scripts/converter.js` | 优先使用模板解析出的配置，无模板时回退到默认 config |
| `package.json` | 新增 `jszip` 依赖 |

### 对模板文件的要求

| 要求 | 原因 |
|------|------|
| **必须 .pptx 格式** | .ppt 是二进制格式，无法用 JSZip 解析 |
| **包含母版布局占位符** | 需有 `<p:ph type="title"/>` / `<p:ph type="body"/>` 等标准占位符，用于识别标题区/内容区位置 |
| **至少 2 种布局** | 封面页布局（ctrTitle + subtitle）+ 内容页布局（title + body） |
| **简单视觉样式** | 纯色背景、矩形边框可完美提取；渐变/阴影/复杂图形无法还原，降级为位置信息 |
| **颜色为显式 RGB** | PptxGenJS 不支持 OOXML 主题色继承，模板中的颜色需为直接 RGB 值而非主题引用 |

### 核心难点

1. **占位符识别**：OOXML 中占位符通过 `<p:sp>` → `<p:nvSpPr>` → `<p:nvPr>` → `<p:ph type="title"/>` 标识，需精确解析 XML 层级
2. **布局映射**：模板通常有多种 layout，需让用户指定哪个 layout 对应封面、哪个对应内容，或自动按占位符类型推断
3. **复杂视觉丢失**：渐变背景、艺术字、3D 效果等 `defineSlideMaster()` API 无法表达，只能提取位置信息
